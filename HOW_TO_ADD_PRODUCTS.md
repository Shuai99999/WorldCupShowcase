# 如何添加和修改商品信息

## 📁 商品配置文件位置

所有商品信息都存储在：**`src/data/products.json`**

---

## 📝 添加新商品

### 步骤 1：准备商品图片

1. 从淘宝或1688下载商品图片
2. 将图片保存到 **`public/products/`** 目录
   - 例如：保存为 `my-product-1.jpg`

### 步骤 2：编辑商品配置文件

1. 打开 **`src/data/products.json`** 文件
2. 在文件中找到最后一个商品后面的 `},`
3. 在其后添加逗号和新的商品对象

### 步骤 3：填写商品信息

按照以下格式添加：

```json
{
  "id": "product-005",
  "name": "Your Product Name",
  "description": "Detailed product description in English. Describe features, materials, uses, etc.",
  "images": [
    "/products/my-product-1.jpg",
    "/products/my-product-2.jpg"
  ],
  "category": "Jerseys",
  "tags": ["Official", "Authentic"]
}
```

### 完整示例（添加第5个商品）

在 `products.json` 文件的最后一个商品后面添加：

```json
,
{
  "id": "product-005",
  "name": "2026 World Cup Official Cap",
  "description": "Premium quality cap with official World Cup logo. Adjustable strap, breathable fabric. Perfect for match day or daily wear.",
  "images": [
    "/products/cap1.jpg"
  ],
  "category": "Accessories",
  "tags": ["Official", "Hat"]
}
```

**注意：** 第一个商品前面不加逗号，后续每个商品前都要加逗号！

---

## ✏️ 修改现有商品

### 修改商品信息

1. 打开 **`src/data/products.json`**
2. 找到要修改的商品（通过 `id` 或 `name` 查找）
3. 直接修改对应的字段值
4. 保存文件

### 修改商品图片

1. 如果要更换图片：
   - 在 `public/products/` 目录替换旧图片（保持相同文件名），或
   - 添加新图片到 `public/products/`，然后修改 `images` 数组中的路径

2. 如果要添加更多图片：
   - 将新图片放到 `public/products/` 目录
   - 在 `images` 数组中添加新路径，例如：
     ```json
     "images": [
       "/products/jersey1.jpg",
       "/products/jersey2.jpg",
       "/products/jersey3.jpg"
     ]
     ```

---

## 📋 字段说明

| 字段 | 是否必需 | 说明 | 示例 |
|------|---------|------|------|
| `id` | ✅ 必需 | 唯一标识符，不能重复 | `"product-001"` |
| `name` | ✅ 必需 | 商品名称（英文） | `"Official Jersey"` |
| `description` | ✅ 必需 | 商品详细描述（英文） | `"High quality jersey..."` |
| `images` | ✅ 必需 | 图片路径数组，至少一张 | `["/products/img1.jpg"]` |
| `category` | ⚪ 可选 | 商品分类 | `"Jerseys"`, `"Accessories"` |
| `tags` | ⚪ 可选 | 标签数组 | `["Official", "Authentic"]` |

---

## 🖼️ 图片相关

### 图片路径格式

- 必须以 `/products/` 开头
- 文件必须放在 `public/products/` 目录
- 示例：`"/products/my-image.jpg"`

### 图片要求

- **格式**：JPG, PNG, WebP 都可以
- **建议尺寸**：宽度 800-1200px
- **建议比例**：4:3 或 3:4
- **文件大小**：建议单张不超过 500KB

### 多张图片

- 第一张图片会作为主图显示
- 如果有超过1张图片，会在右上角显示图片数量
- 可以添加多张图片到 `images` 数组

---

## 🎯 常见分类和标签

### 推荐分类（Category）

- `Jerseys` - 球衣
- `Accessories` - 配饰
- `Memorabilia` - 纪念品
- `Football` - 足球
- `Apparel` - 服装
- `Gifts` - 礼品

### 推荐标签（Tags）

- `Official` - 官方
- `Authentic` - 正品
- `Limited Edition` - 限量版
- `Essential` - 必备
- `Collectible` - 收藏
- `Fan` - 球迷
- `New` - 新品

---

## ⚠️ 注意事项

1. **JSON格式要求**
   - 确保所有字段用双引号 `""`
   - 每个商品对象用花括号 `{}`
   - 最后一个商品后面不要加逗号
   - 确保文件是有效的JSON格式

2. **ID唯一性**
   - 每个商品的 `id` 必须唯一
   - 建议使用递增编号：`product-001`, `product-002`...

3. **图片路径**
   - 图片路径区分大小写
   - 如果图片加载失败，会自动显示占位图

4. **保存后刷新**
   - 修改 `products.json` 后，保存文件
   - 刷新浏览器即可看到更新

---

## 📖 快速示例

### 添加一个简单的商品

```json
{
  "id": "product-006",
  "name": "Team Flag",
  "description": "High quality team flag for match day display.",
  "images": ["/products/flag1.jpg"],
  "category": "Accessories",
  "tags": ["Fan"]
}
```

### 完整的 products.json 文件结构

```json
[
  {
    "id": "product-001",
    "name": "Product 1",
    ...
  },
  {
    "id": "product-002",
    "name": "Product 2",
    ...
  },
  {
    "id": "product-003",
    "name": "Product 3",
    ...
  }
]
```

---

## 🔧 需要帮助？

如果遇到问题：

1. 检查JSON格式是否正确（可以使用在线JSON验证工具）
2. 确认图片路径是否正确
3. 确保商品ID不重复
4. 查看浏览器控制台是否有错误信息

---

**提示：** 你可以随时参考 `src/data/products.template.json` 文件查看配置模板。

